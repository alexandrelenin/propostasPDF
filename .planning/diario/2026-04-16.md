# Diário — 2026-04-16

## O que foi feito

- Implementado template RFID (Cartão de Proximidade): novo `templateType`, campo `mainTableTitle`, formulário condicional, bypass de validação de custo, PDF configurável
- Corrigido bug de dois templates marcados como padrão simultaneamente (`TemplatesView.handleSave` não limpava `isDefault` nos outros)
- Corrigido bug de template padrão não atualizar ao navegar (`App.tsx` re-fetch movido para depender de `location.pathname`)
- Criado `doc/CHANGELOG.md` para rastreabilidade de features

## Decisões tomadas

- "Fechar o plano" = registrar na documentação (`doc/CHANGELOG.md`), não deletar o arquivo
- v2 usará **Go** como backend (já usado no siop), **SQLite** com schema projetado para Postgres, repo separado (`propostasPDF-v2`)
- UI da v2 será **mobile-first** — confirmar com usuário amanhã como usa o sistema (celular ou desktop)
- Design da v2 passa por ferramenta de UI (Google Stitch / v0) antes de codar
- Schema do SQLite já considera features futuras: CRM, assinatura digital gov.br, agente Telegram, templates flexíveis, contratos

## Amanhã / próximos passos

- Perguntar ao usuário como acessa o sistema (mobile ou desktop) para confirmar prioridade do redesign
- Retomar estudo de viabilidade v2 (`~/.claude/plans/nested-frolicking-pearl.md`)
- Decidir quando iniciar o repo v2 e qual fase atacar primeiro (backend Go ou design UI)
- Registrar defeitos adicionais que o usuário queira relatar
